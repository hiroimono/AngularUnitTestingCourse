export class Hero {
  id: number;
  name: string;
  strength: number;

  constructor(id: number, name: string, strength: number) {
    this.id = id
    this.name = name
    this.strength = strength
  }
}
